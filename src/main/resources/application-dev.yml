server:
  port: 8080

spring:
  datasource:
    hikari:
      maximum-pool-size: 10
    url:

datasource:
  url: jdbc:postgresql://localhost:5432/demo
  username: postgres
  password: postgres
  driver: org.postgresql.Driver
  ddl: none
  show_sql: true
